import{_ as a,o as s,c as t,ag as n}from"./chunks/framework.DmiIFzQJ.js";const u=JSON.parse('{"title":"Matrix / Layout Annotation","description":"","frontmatter":{},"headers":[],"relativePath":"development/matrix-annotation.md","filePath":"development/matrix-annotation.md"}'),i={name:"development/matrix-annotation.md"};function o(r,e,l,d,c,p){return s(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="matrix-layout-annotation" tabindex="-1">Matrix / Layout Annotation <a class="header-anchor" href="#matrix-layout-annotation" aria-label="Permalink to &quot;Matrix / Layout Annotation&quot;">​</a></h1><p>This document describes the matrix annotation feature in the keyboard layout editor. It covers the automatic annotation algorithm, the rotation-aware annotation path, duplicate detection, manual drawing, modal states, and how coordinates are applied to keys.</p><p><strong>Relevant source files:</strong></p><table tabindex="0"><thead><tr><th>File</th><th>Role</th></tr></thead><tbody><tr><td><code>src/components/MatrixCoordinatesModal.vue</code></td><td>Modal UI, automatic annotation orchestration</td></tr><tr><td><code>src/utils/matrix-annotation/types.ts</code></td><td>Interfaces: <code>AnnotationAlgorithm</code>, <code>AnnotationResult</code>, <code>AnnotationWarning</code>, <code>AssignmentStatus</code>, <code>MatrixItem</code>, <code>RowColAssignment</code></td></tr><tr><td><code>src/utils/matrix-annotation/cluster.ts</code></td><td>Base cluster algorithm — rounds centers, shifts collisions, selects world vs de-rotated variant</td></tr><tr><td><code>src/utils/matrix-annotation/cluster-symmetry.ts</code></td><td>Symmetry-aware wrapper: detects mirror symmetry and enforces it on cluster output</td></tr><tr><td><code>src/utils/matrix-annotation/build-matrix.ts</code></td><td><code>buildRowsColsFromResult()</code> — converts <code>AnnotationResult</code> to <code>MatrixItem[]</code> with live key references</td></tr><tr><td><code>src/utils/matrix-annotation/index.ts</code></td><td>Barrel export</td></tr><tr><td><code>src/utils/matrix-utils.ts</code></td><td>Rotation grouping, de-rotation, label parsing</td></tr><tr><td><code>src/utils/matrix-validation.ts</code></td><td>Coordinate parsing, duplicate validation, option/choice</td></tr><tr><td><code>src/stores/matrix-drawing.ts</code></td><td>Drawing store (sequences, completed wires, editing)</td></tr><tr><td><code>src/utils/keyboard-geometry.ts</code></td><td><code>getKeyCenter</code> -- rotation-aware center calculation</td></tr><tr><td><code>src/utils/line-intersection.ts</code></td><td><code>findKeysAlongLine</code> -- line sweep for manual drawing</td></tr><tr><td><code>src/components/MatrixAnnotationOverlay.vue</code></td><td>Canvas overlay that renders wires and handles input</td></tr></tbody></table><hr><h2 id="table-of-contents" tabindex="-1">Table of Contents <a class="header-anchor" href="#table-of-contents" aria-label="Permalink to &quot;Table of Contents&quot;">​</a></h2><ol><li><a href="#1-background----via-matrix-coordinates">Background -- VIA Matrix Coordinates</a></li><li><a href="#2-modal-states-and-flows">Modal States and Flows</a></li><li><a href="#3-automatic-annotation-algorithm">Automatic Annotation Algorithm</a></li><li><a href="#4-rotation-aware-annotation">Rotation-Aware Annotation</a></li><li><a href="#5-duplicate-detection-and-resolution">Duplicate Detection and Resolution</a></li><li><a href="#6-manual-drawing">Manual Drawing</a></li><li><a href="#7-applying-coordinates-to-keys">Applying Coordinates to Keys</a></li><li><a href="#8-canvas-overlay-rendering">Canvas Overlay Rendering</a></li></ol><hr><h2 id="_1-background-via-matrix-coordinates" tabindex="-1">1. Background -- VIA Matrix Coordinates <a class="header-anchor" href="#_1-background-via-matrix-coordinates" aria-label="Permalink to &quot;1. Background -- VIA Matrix Coordinates&quot;">​</a></h2><p>Mechanical keyboards use a switch matrix to read key presses. Each physical key is connected to one row wire and one column wire. Pressing a key closes the circuit between its row and column, allowing the controller to detect which key was pressed.</p><p>The <a href="https://www.caniusevia.com/docs/layouts" target="_blank" rel="noreferrer">VIA configurator</a> expects every key in a layout definition to carry a <code>&quot;row,col&quot;</code> annotation in label position 0 (top-left). The matrix annotation feature provides two ways to assign these coordinates: an automatic algorithm based on key geometry, and a manual drawing tool.</p><p>Label format examples:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>&quot;0,0&quot;   -- row 0, column 0  (complete)</span></span>
<span class="line"><span>&quot;2,5&quot;   -- row 2, column 5  (complete)</span></span>
<span class="line"><span>&quot;1,&quot;    -- row 1, column not yet assigned (partial)</span></span>
<span class="line"><span>&quot;,3&quot;    -- row not assigned, column 3 (partial)</span></span></code></pre></div><p>Ghost keys (<code>key.ghost === true</code>) and decal keys (<code>key.decal === true</code>) are excluded from annotation throughout the system. They represent cosmetic elements that do not participate in the electrical matrix.</p><hr><h2 id="_2-modal-states-and-flows" tabindex="-1">2. Modal States and Flows <a class="header-anchor" href="#_2-modal-states-and-flows" aria-label="Permalink to &quot;2. Modal States and Flows&quot;">​</a></h2><p>The modal (<code>MatrixCoordinatesModal.vue</code>) uses a two-step state machine:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>                         +---------+</span></span>
<span class="line"><span>       modal opens -----&gt;| warning |</span></span>
<span class="line"><span>                         +---------+</span></span>
<span class="line"><span>                              |</span></span>
<span class="line"><span>          +-------------------+-------------------+</span></span>
<span class="line"><span>          |                   |                   |</span></span>
<span class="line"><span>    &quot;OK (clear all)&quot;    &quot;Continue&quot;           &quot;Cancel&quot;</span></span>
<span class="line"><span>          |            (partial only)             |</span></span>
<span class="line"><span>    clear labels,           |              close modal</span></span>
<span class="line"><span>    go to draw         go to draw</span></span>
<span class="line"><span>          |            with existing</span></span>
<span class="line"><span>          v            annotations</span></span>
<span class="line"><span>       +------+              |</span></span>
<span class="line"><span>       | draw |&lt;-------------+</span></span>
<span class="line"><span>       +------+</span></span></code></pre></div><h3 id="the-five-open-scenarios" tabindex="-1">The Five Open Scenarios <a class="header-anchor" href="#the-five-open-scenarios" aria-label="Permalink to &quot;The Five Open Scenarios&quot;">​</a></h3><p>When the modal opens, it inspects the current layout state and picks one of five paths. The decision tree lives in the visibility watcher inside <code>MatrixCoordinatesModal.vue</code>.</p><table tabindex="0"><thead><tr><th>#</th><th>Condition</th><th>Behavior</th></tr></thead><tbody><tr><td>1</td><td>Fully annotated <strong>with</strong> invalid duplicates</td><td>Show overlay preview, stay on <code>warning</code> step. User sees a yellow alert about duplicate positions.</td></tr><tr><td>2</td><td>Fully annotated, no invalid duplicates</td><td>Show overlay preview, skip directly to <code>draw</code> step (editing existing annotations).</td></tr><tr><td>3</td><td>No labels at all (blank layout)</td><td>Skip directly to <code>draw</code> step.</td></tr><tr><td>4</td><td>Partially annotated (mix of VIA labels and blanks)</td><td>Stay on <code>warning</code> step, show &quot;Continue&quot; and &quot;Start over&quot; buttons.</td></tr><tr><td>5</td><td>Non-matrix labels present</td><td>Stay on <code>warning</code> step with only &quot;OK (clear all labels)&quot; available.</td></tr></tbody></table><p>&quot;Fully annotated&quot; means <code>keyboardStore.isViaAnnotated</code> is <code>true</code> -- every regular key has a label matching the pattern <code>/^\\d+,\\d+$/</code> in position 0.</p><p>&quot;Invalid duplicates&quot; means two or more keys share the same <code>row,col</code> position without carrying <code>option,choice</code> values in label position 8 (bottom-right). This check uses <code>validateMatrixDuplicates</code> from <code>matrix-validation.ts</code>.</p><h3 id="warning-step-actions" tabindex="-1">Warning Step Actions <a class="header-anchor" href="#warning-step-actions" aria-label="Permalink to &quot;Warning Step Actions&quot;">​</a></h3><ul><li><strong>OK (clear all labels)</strong>: Calls <code>createEmptyLabels()</code> on every regular key, then transitions to <code>draw</code>.</li><li><strong>Continue</strong>: Calls <code>extractMatrixAssignmentsWithPartial</code> to parse the existing labels, loads them into both the modal state and the drawing store via <code>loadExistingAssignments</code>, then transitions to <code>draw</code>.</li><li><strong>Start over</strong>: Same as &quot;OK&quot; -- clears everything first.</li><li><strong>Cancel</strong>: Closes modal, no changes.</li></ul><hr><h2 id="_3-automatic-annotation-algorithm" tabindex="-1">3. Automatic Annotation Algorithm <a class="header-anchor" href="#_3-automatic-annotation-algorithm" aria-label="Permalink to &quot;3. Automatic Annotation Algorithm&quot;">​</a></h2><p>The &quot;Annotate Automatically&quot; button triggers <code>handleAutomaticAnnotation()</code>. The implementation delegates entirely to <code>clusterSymmetryAnnotationAlgorithm</code> from <code>src/utils/matrix-annotation/</code>, which is a two-pass pipeline: a cluster pass (<code>cluster.ts</code>) followed by a symmetry-enforcement pass (<code>cluster-symmetry.ts</code>).</p><h3 id="_3-1-entry-point-and-toraw-requirement" tabindex="-1">3.1 Entry Point and <code>toRaw</code> Requirement <a class="header-anchor" href="#_3-1-entry-point-and-toraw-requirement" aria-label="Permalink to &quot;3.1 Entry Point and \`toRaw\` Requirement&quot;">​</a></h3><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> handleAutomaticAnnotation</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> () </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> rawKeys</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> keyboardStore.keys.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">map</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">k</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> toRaw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(k))</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> result</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> clusterSymmetryAnnotationAlgorithm.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">annotate</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(rawKeys)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">  const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">rows</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">newRows</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">cols</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">newCols</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> buildRowsColsFromResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result, keyboardStore.keys)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  rows.value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> newRows</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  cols.value </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> newCols</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // ... load into drawing store ...</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  applyCoordinatesToKeys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  keyboardStore.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">saveState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">()</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>toRaw(k)</code> strips the Vue reactive <code>Proxy</code> wrapper before passing keys to the algorithm. This is required because <code>structuredClone</code> (used inside <code>cluster.ts</code> to snapshot keys before mutation) cannot clone Proxy objects and throws otherwise. <code>buildRowsColsFromResult</code> receives the original reactive <code>keyboardStore.keys</code> array so the <code>MatrixItem.keySequence</code> arrays hold live references for the drawing store. <code>keyboardStore.saveState()</code> is called unconditionally after every automatic annotation.</p><h3 id="_3-2-cluster-pass-cluster-ts" tabindex="-1">3.2 Cluster Pass (<code>cluster.ts</code>) <a class="header-anchor" href="#_3-2-cluster-pass-cluster-ts" aria-label="Permalink to &quot;3.2 Cluster Pass (\`cluster.ts\`)&quot;">​</a></h3><p>The cluster algorithm is exposed as <code>clusterAnnotationAlgorithm</code> and called internally by the symmetry wrapper.</p><p><strong>Center calculation.</strong> For each regular key (non-ghost, non-decal), <code>getKeyCenter</code> (in <code>keyboard-geometry.ts</code>) computes the key&#39;s center in layout coordinate space. For an unrotated 1u key at <code>(x, y)</code> the center is <code>(x + 0.5, y + 0.5)</code>. For rotated keys the function applies the full 2D rotation transform around <code>(rotation_x, rotation_y)</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>centerX = key.x + key.width / 2</span></span>
<span class="line"><span>centerY = key.y + key.height / 2</span></span>
<span class="line"><span></span></span>
<span class="line"><span>relX = centerX - originX</span></span>
<span class="line"><span>relY = centerY - originY</span></span>
<span class="line"><span></span></span>
<span class="line"><span>rotatedX = relX * cos(angle) - relY * sin(angle)</span></span>
<span class="line"><span>rotatedY = relX * sin(angle) + relY * cos(angle)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>finalX = originX + rotatedX</span></span>
<span class="line"><span>finalY = originY + rotatedY</span></span></code></pre></div><p><strong>Rounding and per-row collision shifting.</strong> <code>Math.round(center.y)</code> gives the tentative row, <code>Math.round(center.x)</code> the tentative column. Keys in the same tentative row are sorted by their tentative column (ties broken by world X). Each key is assigned its tentative column if it is free; if the slot is already taken, the key is shifted to the next free column (<code>while (used.has(c)) c++</code>). No key is ever dropped. The algorithm records the number of shifts as <code>collisions</code> and emits a warning when nonzero.</p><p>After shifting, <code>denseReindex</code> re-maps both row and column values to <code>0, 1, 2, ...</code> based on sorted order, so sparse integer ranges (e.g., rows 0, 1, 3) become contiguous (0, 1, 2).</p><p><strong>World vs de-rotated variant selection.</strong> The cluster pass evaluates two variants:</p><ul><li><strong>Variant A</strong> (world): operates on key centers as-is.</li><li><strong>Variant B</strong> (de-rotated): only considered when at least one rotation group has <code>|angle| &gt; 1e-6</code> and <code>≥ 2 keys</code>. The keys are <code>structuredClone</code>d, then <code>rotation_angle</code> is zeroed on each clone in those groups. The clone is annotated with world centers now computed in the &quot;de-rotated&quot; space.</li></ul><p>Each variant is scored by the tuple <code>(matrixMax, matrixSum, wireLength)</code> where <code>matrixMax = max(numRows, numCols)</code>, <code>matrixSum = numRows + numCols</code>, and <code>wireLength</code> is the sum of consecutive Euclidean gaps within each row (sorted by world X) and each column (sorted by world Y). Lower is better at every level; <code>matrixMax</code> dominates, <code>matrixSum</code> breaks ties, <code>wireLength</code> breaks remaining ties. The better variant&#39;s assignments are written into the <code>AnnotationResult</code>.</p><p><strong>Staggered layout example:</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Physical layout (1u keys):       Center positions:     Rounded:</span></span>
<span class="line"><span></span></span>
<span class="line"><span>+---+---+---+---+                (0.5,0.5) (1.5,0.5)  row=1,col=1  row=1,col=2 ...</span></span>
<span class="line"><span>| Q | W | E | R |</span></span>
<span class="line"><span>+---+---+---+---+</span></span>
<span class="line"><span>  +---+---+---+---+              (0.75,1.5) (1.75,1.5)  row=2,col=1  row=2,col=2 ...</span></span>
<span class="line"><span>  | A | S | D | F |</span></span>
<span class="line"><span>  +---+---+---+---+</span></span>
<span class="line"><span></span></span>
<span class="line"><span>(Stagger shifts x by 0.25u but rounding absorbs it; no collisions)</span></span></code></pre></div><h3 id="_3-3-symmetry-pass-cluster-symmetry-ts" tabindex="-1">3.3 Symmetry Pass (<code>cluster-symmetry.ts</code>) <a class="header-anchor" href="#_3-3-symmetry-pass-cluster-symmetry-ts" aria-label="Permalink to &quot;3.3 Symmetry Pass (\`cluster-symmetry.ts\`)&quot;">​</a></h3><p><code>clusterSymmetryAnnotationAlgorithm</code> wraps the cluster pass with a left-right mirror-symmetry detection and enforcement step.</p><p><strong>Symmetry detection</strong> (<code>detectSymmetry</code>). The bounding-box midline of all regular-key centers is used as the candidate symmetry axis. For every regular key the algorithm attempts to find a mirror twin:</p><ul><li>A key is &quot;on the axis&quot; (self-paired) if <code>|2*axis - cx - cx| &lt; 0.05u</code>. A self-paired key must have <code>|rotation_angle| ≤ 0.5°</code>, otherwise the layout is rejected as asymmetric.</li><li>Otherwise the expected mirror position is <code>(2*axis - cx, cy)</code>. A twin is accepted if <code>|twin.x - mirrorX| ≤ 0.05u</code>, <code>|twin.y - cy| ≤ 0.05u</code>, and the rotations satisfy <code>|rj + ri| ≤ 0.5°</code> (mirror keys should have equal and opposite angles).</li></ul><p>If any regular key has no valid twin, <code>detectSymmetry</code> returns <code>null</code> and the layout is not considered symmetric. Partial symmetry is not accepted.</p><p><strong>Symmetry enforcement</strong> (<code>enforceSymmetry</code>). When symmetry is detected, every right-side key in each pair is overwritten:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>rows[rightIdx] = rows[leftIdx]</span></span>
<span class="line"><span>cols[rightIdx] = totalCols - 1 - cols[leftIdx]</span></span></code></pre></div><p>After rewriting, <code>densify</code> re-maps row and column values to <code>0, 1, 2, ...</code> because the overwrite may introduce gaps. Center keys (those on the axis) are validated first: their column must already satisfy <code>col == totalCols - 1 - col</code> (i.e., <code>totalCols</code> is odd and the key sits on the middle column). If a center key fails this check, enforcement returns <code>null</code> and the algorithm falls back to the cluster output.</p><p><strong>Fallback conditions.</strong> The symmetry pass falls back to returning the cluster output unchanged when:</p><table tabindex="0"><thead><tr><th><code>meta.symmetryFallbackReason</code></th><th>Cause</th></tr></thead><tbody><tr><td><code>&#39;base-not-success&#39;</code></td><td>Cluster pass did not return <code>status: &#39;success&#39;</code></td></tr><tr><td><code>&#39;no-regular-keys&#39;</code></td><td>No regular keys with assigned row/col in cluster result</td></tr><tr><td><code>&#39;unpaired-key&#39;</code></td><td><code>detectSymmetry</code> returned <code>null</code> (at least one key has no mirror twin)</td></tr><tr><td><code>&#39;center-conflict&#39;</code></td><td><code>enforceSymmetry</code> returned <code>null</code> (center key not on symmetry column)</td></tr></tbody></table><p>When fallback occurs, <code>meta.symmetry</code> is set to <code>&#39;fallback&#39;</code>. When enforcement succeeds, <code>meta.symmetry</code> is <code>&#39;enforced&#39;</code> and <code>meta.symmetryAxis</code>, <code>meta.symmetryPairs</code>, and <code>meta.symmetryCenterKeys</code> are populated.</p><h3 id="_3-4-result-structure-and-drawing-store-bridge" tabindex="-1">3.4 Result Structure and Drawing Store Bridge <a class="header-anchor" href="#_3-4-result-structure-and-drawing-store-bridge" aria-label="Permalink to &quot;3.4 Result Structure and Drawing Store Bridge&quot;">​</a></h3><p><code>AnnotationResult</code> is defined in <code>types.ts</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnnotationResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  assignments</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">RowColAssignment</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)[] </span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// index-aligned with input keys</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  status</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AssignmentStatus</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"> // &#39;success&#39; | &#39;partial&#39; | &#39;disqualified&#39;</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  warnings</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> AnnotationWarning</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">[]</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  meta</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Record</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">string</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">unknown</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">&gt;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> RowColAssignment</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  row</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  col</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> null</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>assignments</code> has the same length as the input key array. Ghost and decal keys receive <code>null</code>; regular keys receive a <code>RowColAssignment</code> object.</p><p><code>buildRowsColsFromResult</code> (in <code>build-matrix.ts</code>) converts <code>AnnotationResult</code> into the <code>MatrixItem[]</code> arrays expected by the modal and drawing store. It iterates <code>result.assignments</code>, groups the original reactive key references (passed separately) into <code>rowMap</code> and <code>colMap</code> by row/column number, sorts each group (rows by <code>key.x</code>, columns by <code>key.y</code>), and builds <code>MatrixItem</code> objects with stable <code>id</code> strings and sequential <code>index</code> values:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">rows</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">cols</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> buildRowsColsFromResult</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(result, keyboardStore.keys)</span></span></code></pre></div><p>The live key references are necessary because the drawing store&#39;s <code>completedRows</code> and <code>completedColumns</code> maps hold <code>Key[]</code> arrays that the overlay canvas renders and that <code>applyCoordinatesToKeys</code> reads back.</p><hr><h2 id="_4-rotation-aware-annotation" tabindex="-1">4. Rotation-Aware Annotation <a class="header-anchor" href="#_4-rotation-aware-annotation" aria-label="Permalink to &quot;4. Rotation-Aware Annotation&quot;">​</a></h2><p>Many keyboard layouts include rotated key clusters (e.g., thumb clusters on ergonomic boards). When keys are rotated, their world-coordinate centers shift in ways that can cause <code>Math.round(center.y)</code> to map different keys to the same row even though they are on different logical rows within the cluster.</p><p>Rotation awareness is handled entirely inside <code>cluster.ts</code>. There is no temporary mutation of <code>keyboardStore.keys</code> and no two-step if/else in the modal.</p><h3 id="_4-1-detection" tabindex="-1">4.1 Detection <a class="header-anchor" href="#_4-1-detection" aria-label="Permalink to &quot;4.1 Detection&quot;">​</a></h3><p>Before evaluating variant B, the cluster algorithm calls <code>shouldUseRotationAware(regularClones)</code> on the clone set. This function calls <code>splitLayoutByRotation</code> and returns <code>true</code> when any resulting group has <code>|rotationAngle| &gt; 1e-6</code> <strong>and</strong> at least two keys. If no such group exists, only variant A is evaluated and the de-rotation code path is skipped entirely.</p><h3 id="_4-2-de-rotation-for-variant-b" tabindex="-1">4.2 De-Rotation for Variant B <a class="header-anchor" href="#_4-2-de-rotation-for-variant-b" aria-label="Permalink to &quot;4.2 De-Rotation for Variant B&quot;">​</a></h3><p>When rotation awareness is triggered, a second <code>structuredClone</code> of the key array is created (separate from the variant-A clone). <code>splitLayoutByRotation</code> groups the clone&#39;s regular keys, and <code>rotation_angle</code> is zeroed directly on each clone in groups with a non-zero angle. The clone is never written back to the store; it exists only for the duration of the scoring comparison.</p><p>With rotation zeroed, <code>getKeyCenter</code> computes centers in the key&#39;s &quot;local&quot; coordinate frame, putting keys within the same cluster onto aligned Y values that round to the same row index. This typically produces a more compact <code>(matrixMax, matrixSum)</code> score than world centers when a cluster is rotated relative to the main key field.</p><h3 id="_4-3-variant-selection-and-score" tabindex="-1">4.3 Variant Selection and Score <a class="header-anchor" href="#_4-3-variant-selection-and-score" aria-label="Permalink to &quot;4.3 Variant Selection and Score&quot;">​</a></h3><p>Both variants go through <code>annotateVariant</code>, which performs the same rounding, collision-shifting, and dense-reindexing steps. The score tuple <code>(matrixMax, matrixSum, wireLength)</code> is compared lexicographically; variant B replaces variant A only if it is strictly better. In practice, layouts without rotation always produce identical variants, so variant A is chosen by default and the result carries <code>meta.variant = &#39;world&#39;</code> vs <code>meta.variant = &#39;de-rotated&#39;</code>.</p><hr><h2 id="_5-duplicate-detection-and-resolution" tabindex="-1">5. Duplicate Detection and Resolution <a class="header-anchor" href="#_5-duplicate-detection-and-resolution" aria-label="Permalink to &quot;5. Duplicate Detection and Resolution&quot;">​</a></h2><h3 id="_5-1-what-the-cluster-algorithm-does-with-collisions" tabindex="-1">5.1 What the Cluster Algorithm Does with Collisions <a class="header-anchor" href="#_5-1-what-the-cluster-algorithm-does-with-collisions" aria-label="Permalink to &quot;5.1 What the Cluster Algorithm Does with Collisions&quot;">​</a></h3><p>In the cluster pass, two keys produce a tentative collision when <code>Math.round(center.y)</code> and <code>Math.round(center.x)</code> yield the same values for both. Common causes include:</p><ul><li>Keys very close together (e.g., ISO Enter spanning ~2 positions).</li><li>Rotated clusters where world-coordinate Y values round identically.</li><li>Unusual stagger values that align centers to the same integer.</li></ul><p>Unlike the previous implementation, <strong>no key is dropped</strong>. The <code>annotateVariant</code> function sorts keys in the same row by their tentative column (ties broken by world X), then shifts any colliding key rightward to the next free column slot. The shift count is recorded as <code>variant.collisions</code>. When <code>collisions &gt; 0</code>, the algorithm emits an <code>AnnotationWarning</code> of kind <code>&#39;algorithm-specific&#39;</code> describing how many collisions were resolved.</p><h3 id="_5-2-duplicate-warning-path" tabindex="-1">5.2 Duplicate Warning Path <a class="header-anchor" href="#_5-2-duplicate-warning-path" aria-label="Permalink to &quot;5.2 Duplicate Warning Path&quot;">​</a></h3><p>The old <code>checkForDuplicates</code> + <code>showDuplicateWarning</code> flow that inspected the <code>matrixMap</code> no longer applies to regular key geometry. The duplicate warning path now flows through <code>result.meta?.displayDuplicates</code>:</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> displayDuplicates</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> result.meta?.displayDuplicates </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">as</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  { </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">position</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: string; </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">keys</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: Key[] }[] </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> undefined</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (displayDuplicates) {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  showDuplicateWarning</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(displayDuplicates)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>displayDuplicates</code> is only populated in rare cases where the rotation-aware fallback itself found genuine duplicates — primarily from VIA option/choice imports that produce keys with identical positions before any option differentiation is applied. For normal key geometry, the collision-shifting guarantees <code>displayDuplicates</code> is absent and no warning is shown.</p><h3 id="_5-3-during-manual-drawing-canaddkeytosequence" tabindex="-1">5.3 During Manual Drawing (<code>canAddKeyToSequence</code>) <a class="header-anchor" href="#_5-3-during-manual-drawing-canaddkeytosequence" aria-label="Permalink to &quot;5.3 During Manual Drawing (\`canAddKeyToSequence\`)&quot;">​</a></h3><p>The drawing store prevents duplicates proactively. Before a key is added to the current sequence, <code>canAddKeyToSequence</code> checks:</p><ol><li><p>If drawing a <strong>row</strong> and the candidate key already has a column assignment, it computes what the new row index would be (either the continuing row index or the next free row number) and checks whether any existing key already occupies <code>(newRow, existingCol)</code>.</p></li><li><p>If drawing a <strong>column</strong> and the candidate key already has a row assignment, the same check is done for <code>(existingRow, newCol)</code>.</p></li><li><p>It also checks against other keys in the <strong>current sequence</strong> (which will all receive the same row or column index when the sequence completes).</p></li></ol><p>If any check fails, the key is rejected and the overlay shows it with a red circle and X marker.</p><h3 id="_5-4-via-option-choice-valid-duplicates" tabindex="-1">5.4 VIA Option/Choice (Valid Duplicates) <a class="header-anchor" href="#_5-4-via-option-choice-valid-duplicates" aria-label="Permalink to &quot;5.4 VIA Option/Choice (Valid Duplicates)&quot;">​</a></h3><p>Per the VIA spec, keys <strong>may</strong> share a matrix position when they represent layout variants (e.g., split Backspace vs. 2u Backspace). These keys must carry <code>option,choice</code> values in label position 8 (bottom-right), parsed by <code>parseOptionChoice</code> in <code>matrix-validation.ts</code>.</p><p><code>validateMatrixDuplicates</code> distinguishes between:</p><ul><li><strong>Invalid duplicates</strong>: Multiple keys at the same position where at least one lacks an <code>option,choice</code> label.</li><li><strong>Valid layout options</strong>: Multiple keys at the same position where all carry <code>option,choice</code> labels.</li></ul><p>The overlay only renders wires for &quot;default layout&quot; keys (those with <code>choice === 0</code> or no option/choice at all), filtering via <code>getKeyChoice</code>.</p><hr><h2 id="_6-manual-drawing" tabindex="-1">6. Manual Drawing <a class="header-anchor" href="#_6-manual-drawing" aria-label="Permalink to &quot;6. Manual Drawing&quot;">​</a></h2><h3 id="_6-1-drawing-store-matrix-drawing-ts" tabindex="-1">6.1 Drawing Store (<code>matrix-drawing.ts</code>) <a class="header-anchor" href="#_6-1-drawing-store-matrix-drawing-ts" aria-label="Permalink to &quot;6.1 Drawing Store (\`matrix-drawing.ts\`)&quot;">​</a></h3><p>The Pinia store manages all drawing state:</p><table tabindex="0"><thead><tr><th>State</th><th>Type</th><th>Purpose</th></tr></thead><tbody><tr><td><code>drawingType</code></td><td><code>&#39;row&#39; | &#39;column&#39; | &#39;remove&#39; | null</code></td><td>Current editing mode</td></tr><tr><td><code>currentSequence</code></td><td><code>Key[]</code></td><td>Keys being drawn in the active (incomplete) wire</td></tr><tr><td><code>completedRows</code></td><td><code>Map&lt;number, Key[]&gt;</code></td><td>Finished row wires, keyed by row number</td></tr><tr><td><code>completedColumns</code></td><td><code>Map&lt;number, Key[]&gt;</code></td><td>Finished column wires, keyed by column number</td></tr><tr><td><code>continuingRowIndex</code></td><td><code>number | null</code></td><td>When extending an existing row</td></tr><tr><td><code>continuingColumnIndex</code></td><td><code>number | null</code></td><td>When extending an existing column</td></tr><tr><td><code>insertAfterIndex</code></td><td><code>number | null</code></td><td>T-junction insertion point</td></tr></tbody></table><h3 id="_6-2-drawing-flow" tabindex="-1">6.2 Drawing Flow <a class="header-anchor" href="#_6-2-drawing-flow" aria-label="Permalink to &quot;6.2 Drawing Flow&quot;">​</a></h3><p>Manual drawing follows a two-click interaction model:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>1. User left-clicks a key (first click)</span></span>
<span class="line"><span>   -&gt; Key added to currentSequence</span></span>
<span class="line"><span>   -&gt; If clicked on an existing wire segment/node, set up continuation state</span></span>
<span class="line"><span></span></span>
<span class="line"><span>2. User moves mouse</span></span>
<span class="line"><span>   -&gt; Overlay computes preview: findKeysAlongLine from last key to cursor</span></span>
<span class="line"><span>   -&gt; Legal keys shown as gray dashed preview</span></span>
<span class="line"><span>   -&gt; Illegal keys shown as red dashed preview with X markers</span></span>
<span class="line"><span></span></span>
<span class="line"><span>3. User left-clicks another key (second click)</span></span>
<span class="line"><span>   -&gt; findKeysAlongLine sweeps the line between the two clicked keys</span></span>
<span class="line"><span>   -&gt; All legal intermediate keys are auto-collected</span></span>
<span class="line"><span>   -&gt; Sequence is completed (completeSequence)</span></span>
<span class="line"><span>   -&gt; Wire appears as solid blue (row) or green (column) line</span></span></code></pre></div><p><strong>Cancel drawing</strong>: Right-click or Escape clears the current sequence.</p><h3 id="_6-3-line-sweep-findkeysalongline" tabindex="-1">6.3 Line Sweep (<code>findKeysAlongLine</code>) <a class="header-anchor" href="#_6-3-line-sweep-findkeysalongline" aria-label="Permalink to &quot;6.3 Line Sweep (\`findKeysAlongLine\`)&quot;">​</a></h3><p>When the user draws a line between two keys, <code>findKeysAlongLine</code> (in <code>line-intersection.ts</code>) determines which intermediate keys the line passes through.</p><p>For each candidate key, <code>lineIntersectsKey</code> computes the perpendicular distance from the key&#39;s center to the line segment:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>               line</span></span>
<span class="line"><span>    A --------*----------- B</span></span>
<span class="line"><span>               \\</span></span>
<span class="line"><span>                \\  perpendicular distance</span></span>
<span class="line"><span>                 \\</span></span>
<span class="line"><span>                  * key center</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    If distance &lt;= threshold, key is &quot;along the line&quot;</span></span></code></pre></div><p>The threshold is derived from the key&#39;s dimensions and modulated by a <code>sensitivity</code> parameter (0.0 = most permissive, 1.0 = strictest; default 0.3 in the store). For non-rotated keys the threshold considers the key dimension perpendicular to the line direction. For rotated keys a conservative diagonal-based radius is used.</p><p>Keys beyond the segment endpoints receive special treatment: the algorithm checks whether the endpoint falls within the key&#39;s bounding box rather than using the distance threshold, preventing wide keys past the ends from being incorrectly collected.</p><p>Results are sorted by distance from the start point so they appear in traversal order.</p><h3 id="_6-4-row-and-column-number-assignment" tabindex="-1">6.4 Row and Column Number Assignment <a class="header-anchor" href="#_6-4-row-and-column-number-assignment" aria-label="Permalink to &quot;6.4 Row and Column Number Assignment&quot;">​</a></h3><p>New wires receive the next free index. <code>findNextFreeRowNumber</code> / <code>findNextFreeColumnNumber</code> search from 0 upward to find the first unused number, filling gaps left by deletions.</p><h3 id="_6-5-continuing-and-extending-wires-t-junctions" tabindex="-1">6.5 Continuing and Extending Wires (T-Junctions) <a class="header-anchor" href="#_6-5-continuing-and-extending-wires-t-junctions" aria-label="Permalink to &quot;6.5 Continuing and Extending Wires (T-Junctions)&quot;">​</a></h3><p>Clicking on an existing wire&#39;s node or segment starts a &quot;continuation&quot;:</p><ul><li><code>continuingRowIndex</code> / <code>continuingColumnIndex</code> is set to the wire&#39;s index.</li><li><code>insertAfterIndex</code> and <code>insertionAnchorKey</code> record the clicked position.</li><li>When the sequence completes, new keys are <strong>merged</strong> into the existing wire.</li></ul><p>The merge uses <code>findOptimalInsertion</code>, which evaluates four candidate orderings and picks the one with the lowest total path cost (sum of Euclidean distances between consecutive keys):</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Given existing wire:  [A] --- [B] --- [C]</span></span>
<span class="line"><span>Insert point: after B</span></span>
<span class="line"><span>New keys: [X, Y]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Candidates:</span></span>
<span class="line"><span>  1. forward-after:   [A, B, X, Y, C]</span></span>
<span class="line"><span>  2. forward-before:  [A, X, Y, B, C]</span></span>
<span class="line"><span>  3. reversed-after:  [A, B, Y, X, C]</span></span>
<span class="line"><span>  4. reversed-before: [A, Y, X, B, C]</span></span>
<span class="line"><span></span></span>
<span class="line"><span>Winner: minimum total Euclidean path cost</span></span>
<span class="line"><span>Tie-break: prefer forward-after &gt; forward-before &gt; reversed-after &gt; reversed-before</span></span></code></pre></div><h3 id="_6-6-remove-mode" tabindex="-1">6.6 Remove Mode <a class="header-anchor" href="#_6-6-remove-mode" aria-label="Permalink to &quot;6.6 Remove Mode&quot;">​</a></h3><p>The drawing mode toggle includes a &quot;Remove&quot; option. In this mode:</p><table tabindex="0"><thead><tr><th>Action</th><th>Ctrl held?</th><th>Result</th></tr></thead><tbody><tr><td>Click node</td><td>No</td><td>Remove that single key from its row or column</td></tr><tr><td>Click node (overlap)</td><td>No</td><td>Remove key from both its row and column</td></tr><tr><td>Click segment</td><td>No</td><td>Split the wire at that segment boundary (creates two wires)</td></tr><tr><td>Click segment</td><td>Yes</td><td>Remove the entire wire</td></tr></tbody></table><p>Segment splitting uses <code>splitRowAtSegment</code> / <code>splitColumnAtSegment</code> in the drawing store. These functions:</p><ol><li>Divide the key array at the segment boundary.</li><li>Keep the first portion under the original wire number.</li><li>Create a new wire with the next free number for the second portion.</li><li>Update key labels to reflect the new wire numbers.</li></ol><h3 id="_6-7-renumbering" tabindex="-1">6.7 Renumbering <a class="header-anchor" href="#_6-7-renumbering" aria-label="Permalink to &quot;6.7 Renumbering&quot;">​</a></h3><p>Users can change a row or column number by hovering over its wire and typing digits, then pressing Enter.</p><ul><li><code>renumberRow(old, new)</code> and <code>renumberColumn(old, new)</code> handle the swap.</li><li>If the target number is already in use, the two wires are <strong>swapped</strong> (neither is lost).</li><li>Key labels are updated immediately via <code>updateKeyLabel</code>.</li></ul><hr><h2 id="_7-applying-coordinates-to-keys" tabindex="-1">7. Applying Coordinates to Keys <a class="header-anchor" href="#_7-applying-coordinates-to-keys" aria-label="Permalink to &quot;7. Applying Coordinates to Keys&quot;">​</a></h2><p><code>applyCoordinatesToKeys()</code> is called after any change to the row/column assignments. It builds two lookup maps (<code>keyToRow</code>, <code>keyToCol</code>) from the modal&#39;s <code>rows</code> and <code>cols</code> arrays, then iterates every regular key:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>for each key (excluding ghost/decal):</span></span>
<span class="line"><span>    rowIndex = keyToRow.get(key)</span></span>
<span class="line"><span>    colIndex = keyToCol.get(key)</span></span>
<span class="line"><span></span></span>
<span class="line"><span>    if both defined:  key.labels[0] = &quot;row,col&quot;</span></span>
<span class="line"><span>    if row only:      key.labels[0] = &quot;row,&quot;</span></span>
<span class="line"><span>    if col only:      key.labels[0] = &quot;,col&quot;</span></span>
<span class="line"><span>    if neither:       key.labels[0] = &quot;&quot;</span></span></code></pre></div><p>This is called:</p><ul><li>Immediately after automatic annotation completes.</li><li>On every drawing store change (via watcher, unless <code>skipNextSync</code> is set).</li><li>When the annotation is detected as complete.</li></ul><h3 id="sync-watcher" tabindex="-1">Sync Watcher <a class="header-anchor" href="#sync-watcher" aria-label="Permalink to &quot;Sync Watcher&quot;">​</a></h3><p>A Vue <code>watch</code> on the drawing store&#39;s completed rows/columns triggers <code>syncDrawingsToModal</code> followed by <code>applyCoordinatesToKeys</code>:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>watch([completedRows.size, completedColumns.size, totalKeysInRows, totalKeysInColumns])</span></span>
<span class="line"><span>  -&gt; syncDrawingsToModal()      // convert store Maps to modal&#39;s MatrixItem[]</span></span>
<span class="line"><span>  -&gt; applyCoordinatesToKeys()   // write labels to keys</span></span></code></pre></div><p>The <code>skipNextSync</code> flag prevents re-application after context menu removals, where the removal handler has already updated labels directly.</p><hr><h2 id="_8-canvas-overlay-rendering" tabindex="-1">8. Canvas Overlay Rendering <a class="header-anchor" href="#_8-canvas-overlay-rendering" aria-label="Permalink to &quot;8. Canvas Overlay Rendering&quot;">​</a></h2><p><code>MatrixAnnotationOverlay.vue</code> renders a <code>&lt;canvas&gt;</code> element layered on top of the keyboard canvas. It draws:</p><table tabindex="0"><thead><tr><th>Element</th><th>Color</th><th>Style</th></tr></thead><tbody><tr><td>Completed row wires</td><td>Blue (#007bff)</td><td>Solid line, filled circles at nodes</td></tr><tr><td>Completed column wires</td><td>Green (#28a745)</td><td>Solid line, filled circles at nodes</td></tr><tr><td>Active drawing sequence</td><td>Orange/yellow (#ffc107)</td><td>Thicker solid line</td></tr><tr><td>Preview (legal keys)</td><td>Gray (50% opacity)</td><td>Dashed line, semi-transparent circles</td></tr><tr><td>Preview (illegal keys)</td><td>Red (80% opacity)</td><td>Dashed line, circles with X marks</td></tr><tr><td>Hovered wire</td><td>Blue or Green</td><td>Thicker line (4px vs 2px), larger circles</td></tr><tr><td>Hovered node</td><td>Yellow (#ffc107)</td><td>Semi-transparent highlight circle</td></tr></tbody></table><p>All rendering uses the same coordinate transform as the main keyboard canvas (zoom + pan offset), so wires align exactly with keys.</p><p>Only &quot;default layout&quot; keys (no <code>option,choice</code> or <code>choice === 0</code>) have their wires rendered. Alternative layout option keys are stored in the wire arrays but filtered out during rendering.</p><hr><h2 id="architecture-diagram" tabindex="-1">Architecture Diagram <a class="header-anchor" href="#architecture-diagram" aria-label="Permalink to &quot;Architecture Diagram&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>| MatrixCoordinatesModal    |  (orchestration, UI, auto-annotation)</span></span>
<span class="line"><span>+-------------+-------------+</span></span>
<span class="line"><span>              |</span></span>
<span class="line"><span>              | calls annotate(), buildRowsColsFromResult()</span></span>
<span class="line"><span>              v</span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>| matrix-annotation/        |</span></span>
<span class="line"><span>|   cluster-symmetry.ts     |  clusterSymmetryAnnotationAlgorithm</span></span>
<span class="line"><span>|   cluster.ts              |  clusterAnnotationAlgorithm</span></span>
<span class="line"><span>|   build-matrix.ts         |  buildRowsColsFromResult</span></span>
<span class="line"><span>|   types.ts                |  AnnotationResult, MatrixItem, ...</span></span>
<span class="line"><span>+-------------+-------------+</span></span>
<span class="line"><span>              |</span></span>
<span class="line"><span>              | uses</span></span>
<span class="line"><span>              v</span></span>
<span class="line"><span>+---------------------------+     +---------------------------+</span></span>
<span class="line"><span>| keyboard-geometry.ts      |     | matrix-utils.ts           |</span></span>
<span class="line"><span>| - getKeyCenter            |     | - splitLayoutByRotation   |</span></span>
<span class="line"><span>+---------------------------+     +---------------------------+</span></span>
<span class="line"><span>              |</span></span>
<span class="line"><span>              | reads/writes</span></span>
<span class="line"><span>              v</span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>| matrix-drawing store      |  (Pinia: sequences, completed wires, editing)</span></span>
<span class="line"><span>+-------------+-------------+</span></span>
<span class="line"><span>              |</span></span>
<span class="line"><span>              | observed by</span></span>
<span class="line"><span>              v</span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>| MatrixAnnotationOverlay   |  (canvas rendering, mouse/keyboard input)</span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>              |</span></span>
<span class="line"><span>              | uses</span></span>
<span class="line"><span>              v</span></span>
<span class="line"><span>+---------------------------+     +---------------------------+</span></span>
<span class="line"><span>| keyboard-geometry.ts      |     | line-intersection.ts      |</span></span>
<span class="line"><span>| - getKeyCenter            |     | - findKeysAlongLine       |</span></span>
<span class="line"><span>| - getKeyDistance          |     | - lineIntersectsKey       |</span></span>
<span class="line"><span>+---------------------------+     +---------------------------+</span></span>
<span class="line"><span></span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>| matrix-validation.ts      |  (coordinate parsing, duplicate validation,</span></span>
<span class="line"><span>|                           |   option/choice support)</span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>              |</span></span>
<span class="line"><span>              v</span></span>
<span class="line"><span>+---------------------------+</span></span>
<span class="line"><span>| keyboard store            |  (isViaAnnotated, hasInvalidMatrixDuplicates,</span></span>
<span class="line"><span>| (Pinia)                   |   key data, labels)</span></span>
<span class="line"><span>+---------------------------+</span></span></code></pre></div>`,140)])])}const g=a(i,[["render",o]]);export{u as __pageData,g as default};
